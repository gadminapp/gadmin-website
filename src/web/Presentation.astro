---
import leftPresentation from '@/assets/img/presentation/left-img.png'
import centerPresentation from '@/assets/img/presentation/main.png'
import rightPresentation from '@/assets/img/presentation/right-img.png'
import { Image } from 'astro:assets'
---

<div class='presentation'>
  <div class='wrapper'>
    <h2 class='presentation-title' data-aos='flip-down'>
      Crea tu <span>tienda online</span> sin complicaciones técnicas
    </h2>
    <p class='presentation-subtitle' data-aos='fade-up' data-aos-delay='200'>
      Olvídate de hostings complicados y códigos imposibles.
      <br />
      Transforma tu negocio en una tienda digital que impresione a tus clientes.
    </p>
    <div class='presentation-images' data-aos='zoom-in'>
      <Image
        class='presentation-image presentation-image-left'
        src={leftPresentation}
        width={400}
        height={780}
        alt='Panel de configuración fácil para tu tienda online'
      />
      <Image
        class='presentation-image presentation-image-center'
        src={centerPresentation}
        width={1362}
        height={780}
        alt='Editor visual para crear tu tienda online sin código'
      />
      <Image
        class='presentation-image presentation-image-right'
        src={rightPresentation}
        width={400}
        height={780}
        alt='Personalización avanzada de tu catálogo digital'
      />
    </div>
    <div class='presentation-features'>
      <div class='presentation-feature' data-aos='fade-up' data-aos-delay='100'>
        <div class='feature-icon'>🛍️</div>
        <strong>Tu catálogo digital en minutos</strong> Sube tus productos, configura precios y listo.
        Todo visual, sin programar.
      </div>
      <div class='presentation-feature' data-aos='fade-up' data-aos-delay='200'>
        <div class='feature-icon'>🎨</div>
        <strong>Diseño que vende</strong> Plantillas optimizadas para conversión, diseñadas específicamente
        para tiendas online exitosas.
      </div>
      <div class='presentation-feature' data-aos='fade-up' data-aos-delay='300'>
        <div class='feature-icon'>🚀</div>
        <strong>Desde cero a vendiendo</strong> Tu tienda lista y funcionando en el mismo día.
        <br />
        Sin esperas ni complicaciones.
      </div>
    </div>
  </div>
</div>

<style>
  .presentation {
    background-color: var(--neutral-02);
    padding: 4rem 1rem;
  }

  .presentation-title {
    margin: 0;
    margin-bottom: 1rem;
    color: var(--shade-03);
    text-align: center;
    font-size: var(--64-pt);
    font-weight: 800;
    line-height: 1.1;
  }

  .presentation-subtitle {
    margin: 0 auto 3rem auto;
    color: var(--shade-04);
    text-align: center;
    font-size: 1.2rem;
    max-width: 700px;
    line-height: 1.4;
    font-weight: 400;
  }

  .presentation-title span {
    background: linear-gradient(to right, var(--primary), var(--shade-03));
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
  }

  .presentation-images {
    display: grid;
    grid-template-columns: 1fr;
    gap: 3rem;
    place-items: center;
    padding: 5rem 1rem;
    padding-bottom: 0rem;
    margin-top: 2rem;
  }

  .presentation-image {
    width: 90%;
    height: auto;
    border-radius: 0.5rem;
    background-color: transparent;
    box-shadow: 0 2px 5px rgba(0, 0, 0, 0.15);
  }

  .presentation-image-left,
  .presentation-image-right {
    position: absolute;
    top: -1.5rem;
    left: 0rem;
    width: 28%;
    box-shadow: 4px 8px 8px hsla(235, 72%, 10%, 0.35);
    opacity: 0;
    transition:
      opacity 0.3s ease,
      left 0.3s ease,
      right 0.3s ease;
  }

  .presentation-images:has(.presentation-image-center:hover) .presentation-image-left,
  .presentation-images:has(.presentation-image-center:hover) .presentation-image-right,
  .presentation-image-left:hover,
  .presentation-image-right:hover {
    opacity: 1;
  }

  .presentation-images:has(.presentation-image-center:hover) .presentation-image-left,
  .presentation-image-left:hover {
    left: 1rem;
  }

  .presentation-images:has(.presentation-image-center:hover) .presentation-image-right,
  .presentation-image-right:hover {
    right: 1rem;
  }

  .presentation-image-right {
    left: inherit;
    right: 0rem;
    box-shadow: -4px 8px 8px hsla(235, 72%, 10%, 0.35);
  }

  .presentation-features {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 2rem;
    font-size: 1.125rem;
  }

  .presentation-feature {
    border-radius: 1rem;
    padding: 2rem 1.5rem;
    text-align: center;
    color: var(--shade-04);
    line-height: 1.6;
    transition: transform 0.3s linear;
    transition-delay: 0s;
  }

  .presentation-feature:hover {
    transform: translateY(-16px);
  }

  .feature-icon {
    font-size: 2.5rem;
    margin-bottom: 1rem;
    display: block;
  }

  .presentation-feature strong {
    color: var(--shade-03);
    display: block;
    margin-bottom: 0.5rem;
    font-size: 1.2rem;
    background: linear-gradient(to right, var(--primary), var(--shade-03));
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
  }

  @media screen and (max-width: 768px) {
    .presentation-title {
      font-size: var(--48-pt);
    }

    .presentation-subtitle {
      font-size: var(--20-pt);
      margin-bottom: 2rem;
    }

    .presentation-images {
      grid-template-columns: 1fr;
      padding: 2rem 0;
    }

    .presentation-image-center {
      width: 100%;
    }

    .presentation-image-left,
    .presentation-image-right {
      display: none;
    }

    .presentation-features {
      grid-template-columns: 1fr;
      gap: 1.5rem;
      text-align: center;
      margin-top: 1rem;
    }

    .presentation-feature {
      padding: 1.5rem 1rem;
    }

    .feature-icon {
      font-size: 2rem;
      margin-bottom: 0.8rem;
    }
  }

  @media screen and (min-width: 768.1px) {
    .presentation-image-left,
    .presentation-image-right {
      transform: translateY(0rem);
      animation: move cubic-bezier(0.4, 0, 0.2, 1) forwards;
      animation-timeline: scroll(nearest block);
      will-change: transform;
      backface-visibility: hidden;
    }

    @keyframes move {
      0% {
        transform: translateY(0rem);
      }
      100% {
        transform: translateY(5rem);
      }
    }
  }
</style>
