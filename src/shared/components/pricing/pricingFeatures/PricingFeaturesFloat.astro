---
import PricingFeaturesCard from '@/shared/components/pricing/pricingFeatures/PricingFeaturesCard.astro'
---

<section class='table'>
  <div class='table__item--features'>
    <span class='table__section-name'>FUNCIONALIDADES</span>
    <span class='table__name'>Funcionalidades por plan</span>
    <span class='table__description'>Encuentra el plan que más le convenga a tu tienda.</span>
  </div>
  <PricingFeaturesCard card='bronze' name='Bronce 🤎' description='10$/mes' />
  <PricingFeaturesCard card='silver' name='Plata 🩶' description='20$/mes' />
  <PricingFeaturesCard card='gold' name='Oro 💛' description='99$/mes' />
</section>

<style>
  .table {
    display: grid;
    grid-template-columns: 1.2fr repeat(3, 1fr);
    gap: 0.5rem;
    place-items: center;
    border-radius: 1rem;
    border-bottom-left-radius: 0;
    border-bottom-right-radius: 0;
    border-bottom: 1px solid var(--shade-03);
    background-color: white;
    padding: 1rem;
    position: sticky;
    position: -webkit-sticky;
    top: 5rem;
    width: 100%;
    overflow-x: auto; /* Agrega scroll horizontal si es necesario */
  }

  .table__item--features {
    display: flex;
    flex-direction: column;
    gap: 1rem;
    padding: 1rem;
    width: 100%;
    height: 100%;
    border-top-left-radius: 1rem;
    border-bottom-left-radius: 1rem;
  }

  .table__section-name {
    font-size: 1.1rem;
    background-image: linear-gradient(to right, var(--primary), var(--secondary));
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
  }

  .table__name {
    font-size: 1.6rem;
    color: var(--shade-03);
  }

  .table__description {
    color: var(--neutral-08);
  }

  @media screen and (max-width: 768px) {
    .table {
      grid-template-columns: 1fr;
      gap: 1rem;
      padding: 0.5rem;
    }
    .table__item--features {
      border-radius: 1rem;
      border-bottom: 1px solid var(--shade-03);
    }
  }

  @media screen and (max-width: 480px) {
    .table {
      padding: 0.5rem 0.25rem;
    }
    .table__section-name {
      font-size: 1rem;
    }
    .table__name {
      font-size: 1.4rem;
    }
  }
</style>
